name: preview-notifications
on:
  repository_dispatch:
    types: [Kustomization/epp-deploy.flux-system]

jobs:
  notification-tests:
    runs-on: ubuntu-latest
    steps:
    - name: Notify
      run: echo "this is a notification"
